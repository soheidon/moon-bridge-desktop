{
  "name": "moonbridge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers/cmd/workers-assets-gen && tinygo build -o ./build/app.wasm -target wasm ./cmd/cloudflare",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.86.0"
  }
}
